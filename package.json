{
  "name": "homarr-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for managing Homarr dashboard",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "homarr-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:e2e": "docker compose -f docker-compose.test.yml up -d && vitest run; docker compose -f docker-compose.test.yml down -v"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.2"
  }
}
